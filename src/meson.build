beat_sources = [
  '__init__.py',
  'application.py',
  'player.py',
  'settings.py'
]

install_data(beat_sources, install_dir: moduledir)
install_subdir('window', install_dir: moduledir)
install_subdir('widgets', install_dir: moduledir)
install_subdir('utils', install_dir: moduledir)
